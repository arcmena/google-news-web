import styled from 'styled-components'

import {
  FontSizeMd,
  FontWeightMedium,
  LineHeightMedium,
  NeutralColor4
} from '../../../styles/design-tokens'

export const SubtitleContainer = styled.h6`
  font-size: ${FontSizeMd};
  line-height: ${LineHeightMedium};

  font-weight: ${FontWeightMedium};
  color: ${NeutralColor4};
`
